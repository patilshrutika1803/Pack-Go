export function GroupNotifications({ notifications, readAll, read, openNotification, loading, error }) { return <section className="panel"><h2>Notifications</h2>{loading && <p>Loading notifications...</p>}{error && <p className="form-error" role="alert">{error}</p>}<button className="button button-quiet" onClick={readAll}>Mark all read</button>{!loading && notifications.length === 0 && <p>No notifications yet.</p>}{notifications.map(item => <article key={item.id}><strong>{item.read_at ? '' : 'New: '}</strong>{item.event_type}<small> {new Date(item.created_at).toLocaleString()}</small>{!item.read_at && <button className="button button-quiet" onClick={() => read(item.id)}>Mark read</button>}{item.trip_id && <button className="button button-quiet" onClick={() => openNotification(item)}>Open</button>}</article>)}</section> }
